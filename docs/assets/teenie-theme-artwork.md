# Teenie Friends artwork

Teenie Friends (`teenie`) uses the existing Teenieping portraits as identity references, with new action illustrations matching their rounded 3D style. Heartsping welcomes and encourages, while Tidyping, Artping, Mightyping, Hurryping, Yumyumping, Scrubping, Sleeping, Timidping and Toyping illustrate the daily routines.

Weeping provides the sad crying reaction in `quiz-incorrect.webp`, replacing Heartsping's cheerful retry-arrow pose. The local character list identifies Weeping with sadness and tear magic, making this the closest match for the Princess crying state. The redraw preserves green leafy hair, amber eyes, cream body and leaf umbrella, with visible blue tears and a downturned mouth. The built-in imagegen prompt and source are recorded in [quiz-incorrect.json](teenie-theme-corrections/quiz-incorrect.json). Its genuine alpha is preserved in export; no local background removal was needed.

The artwork inventory comprises 27 character scenes, 16 seasonal landscapes, 67 animal/insect ability analogies, 9 glossy control/plate/bottle redraws, and 16 retained native SVG controls. The edit/delete buttons, calendar, active-state and locked-reward icons, and dinner plate now use transparent Teenieping-style WebP props; their original SVGs remain available. The screen base is soft pink behind white cards, with purple/pink interface accents; existing illustrations retain their original colours. School-day and dinner scenes serve multiple roles. The existing Heartsping portrait and Giftping gift prop are reused by URL. The scenery uses the lilac castle as its architectural reference; production imports use optimized WebP scenery rather than the source castle PNG.

Character scenes, props, ability illustrations and SVG controls have transparent backgrounds. The seasonal landscapes are deliberately opaque. The exporter rejected 46 ability masters with painted checkerboards. After image-tool corrections also returned opaque files, the user explicitly authorized local background removal on 7 September 2026.

Character illustrations must not contain check marks or approval badges. The celebration (`quiz-correct.webp`, also used for giving stars) and hydration success (`drink-success.webp`) images were edited with the built-in image tool to remove their entire check-mark badges. Their revised transparent exports keep the characters and action props; the original masters remain recorded in the correction history. Both edited masters needed the previously authorized local transparency cleanup, and were reviewed on light and dark backgrounds.

`scripts/remove-teenie-backgrounds.py` preserves the original masters and creates separate RGBA masters under `output/teenie-theme/local-alpha/`. It uses U2Net to protect white subject interiors, removes connected neutral checkerboard regions, repairs pale fur outlines, and removes the white matte along a narrow antialiased edge. The firefly's yellow aura uses chroma extraction to remove the checkerboard beneath its light. Each correction record retains its input source, method and user authorization. The cutouts were inspected against plum and pale lilac backgrounds, including ears, white fur, detached props, wings and web threads.

Files are installed under `src/assets/themes/teenie/` and `src/assets/animal-abilities-generic/teenie/`. `src/ui/themeAssets.ts` maps semantic UI roles to assets for Princess and Teenie Friends. The `bravePrincess` stored chore key remains compatible and is presented as “Being brave” in the Teenie theme. Animal and insect facts and the Teenieping quiz collection are unchanged; the new ability pictures are visual analogies performed by Mightyping.

The full prompts, reference paths, selected master paths, export dimensions, alpha checks and byte sizes are in [teenie-theme-artwork.json](teenie-theme-artwork.json). Original generation records are in `teenie-theme-generation/`; transparency corrections are in `teenie-theme-corrections/`. Generation used the built-in `image_gen` tool. Raw masters remain at their recorded original paths.

Run `python scripts/prepare-teenie-theme.py` to reproduce production WebP exports from the recorded masters. Pillow is required. The exporter checks that every transparent master has an actual alpha channel with fully clear and fully opaque pixels and sufficient clear area, then verifies that WebP encoding preserves the resized alpha channel exactly. It produces dark-background contact sheets and a rejection report under `output/teenie-theme/`, and exits unsuccessfully if the set is incomplete or an opaque master remains.

The three flask states now use a matching glossy bottle with a pink heart cap, lavender collar/base, gold trim and blue water. Full, two-thirds and one-third exports are `flask-full.webp`, `flask-twothirds.webp` and `flask-onethird.webp`; the original SVGs remain available. Their built-in imagegen prompts are in `teenie-theme-generation/control-flask-{full,twothirds,onethird}.json`. Image-tool edits removed the checker pattern inside the glass; `python scripts/prepare-teenie-flasks.py` reproduces the approved exterior background removal before the main exporter. The middle image was selected for its actual two-thirds level despite the generation prompt requesting one third; a further edit produced the distinctly lower final one-third state. All three transparent exports were visually inspected, and seven water/toilet and asset-registry tests passed.

To reproduce local corrections first, run `python scripts/remove-teenie-backgrounds.py` with Pillow, NumPy, SciPy and `rembg[cpu]` installed. The script reuses cached U2Net masks under `tmp/teenie-alpha-masks/`; its model cache is `tmp/insect-bg-models/`. Production files are committed assets and do not require these Python dependencies at runtime.

Validation is recorded in the implementation plan after the final artwork and application checks.

The edit/delete redraws used the built-in image generator with Artping's paintbrush and Tidyping's broom as material/style references. Generation records are `teenie-theme-generation/control-edit.json` and `control-delete.json`. The pencil already had genuine alpha. Run `python scripts/prepare-teenie-control-icons.py` to recreate the delete icon's user-authorized local checkerboard removal before the main exporter. The source image and original SVG are preserved.

The plate redraw uses a glossy lavender rim, pink inset band and hearts, a subtle gold ring, and an opaque pearl-white centre. The exact built-in image-generation prompt is in `teenie-theme-generation/control-plate.json`. `python scripts/prepare-teenie-plate.py` removes only the exterior matte using the existing local-edit authorization and aligns the circular artwork with the portion clipping masks. The app draws thin lilac portion boundaries dynamically; no spokes or check marks are embedded in the image. The full plate and bite-removal state were checked in the real `DinnerCountdown` component.

The active-state redraw is a glossy pink heart jewel with a lavender setting and gold trim, without a check mark. Its exact built-in image-generation prompt is in `teenie-theme-generation/control-active.json`; the generated alpha is preserved in `active.webp`. Teenie active-state images use `data-artwork-fit="contain"` so the full jewel fits inside wide action buttons instead of inheriting the oversized illustration treatment. The selected/disabled button was visually reviewed in the real `StandardActionList` component.

The calendar redraw uses a glossy candy-pink casing, lavender bindings, a pearl-white page and pink heart jewels without a check mark. Its built-in generation prompt is in `teenie-theme-generation/control-calendar.json`. Run `python scripts/prepare-teenie-calendar.py` before the main exporter to reproduce the user-authorized removal of the exterior checkerboard; the enclosed white page stays opaque. The original calendar SVG is retained. Asset registry and tab navigation checks passed (11 tests).

The locked-reward redraw is a pink heart-jewel padlock with a lavender shackle, gold trim and purple keyhole. Its built-in generation prompt is in `teenie-theme-generation/control-lock.json`; the generated alpha, including the shackle opening, is preserved in `locked-reward.webp`. The original SVG remains available. The Teenie reward button uses the standard oversized artwork treatment, showing roughly one third of the lock's height inside the button. Asset registry, standard action list and reward creation checks passed (11 tests), along with TypeScript checks.

The toilet-check pair now follows the Princess action poses using Scrubping: `notpeepee.webp` stands with crossed legs, hands together and a worried expression; `didpeepee.webp` sits on a white toilet with a pink seat, hands on lap. Both are fully clothed, with no check marks or approval badges. Built-in image-generation prompts and original sources are recorded in [notpeepee.json](teenie-theme-corrections/notpeepee.json) and [didpeepee.json](teenie-theme-corrections/didpeepee.json). The seated image preserves generated alpha. Run `python scripts/prepare-teenie-toilet.py` before the exporter to reproduce the standing image's approved local background removal, including protection for its white clothing, bow and belt. Both final transparent exports were visually reviewed; all seven water/toilet and asset-registry tests passed.
